name: Tests

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main"]

jobs:

  # ── Fast: no GROBID needed, runs on every commit ──────────────────
  unit-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: pip install -r requirements.txt pytest

      - name: Run unit tests (no GROBID)
        run: pytest tests/unit/unit_tests.py -v

  # ── Slow: real GROBID, runs on push to main or nightly ────────────
  integration-tests:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event_name == 'schedule'

    services:
      grobid:
        image: grobid/grobid:0.8.2-crf
        ports:
          - 8070:8070
        env:
          JAVA_OPTS: "-XX:-UseContainerSupport"
          GROBID_OPTS: "-XX:-UseContainerSupport"
        options: >-
          --health-cmd "wget -qO- http://localhost:8070/api/isalive || exit 1"
          --health-interval 15s
          --health-timeout 10s
          --health-retries 15
          --health-start-period 60s

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12.3"

      - name: Check GROBID container logs
        if: always()
        run: docker logs $(docker ps -aq --filter "ancestor=grobid/grobid:0.8.2-crf")

      - name: Install dependencies
        run: pip install -r requirements.txt pytest

      - name: Wait for GROBID to be ready
        run: |
          echo "Waiting for GROBID..."
          for i in $(seq 1 30); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8070/api/isalive || true)
            if [ "$STATUS" = "200" ]; then
              echo "GROBID is up after $i attempts!"
              exit 0
            fi
            echo "Attempt $i — got HTTP $STATUS, retrying in 10s..."
            sleep 10
          done
          echo "GROBID never became healthy"
          exit 1

      - name: Run integration tests (real GROBID)
        run: pytest tests/integration/int_tests.py -v
        env:
          GROBID_URL: http://localhost:8070
